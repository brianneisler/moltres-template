import { Stack } from 'immutable'

/**
 * @private
 * @function
 * @since v0.1.0
 * @category lang.util.js
 * @param {*} value
 * @returns {ImmutableStack} A new ImmutableStack
 * @example
 */
const ImmutableStack = Stack

export default ImmutableStack
