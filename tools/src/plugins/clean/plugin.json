{
  "name": "clean",
  "command": "clean",
  "description": "removes build artifacts from a moltres project",
  "options": {
    "only": {
      "option": "-o, --only",
      "description": "Only clean the specified target and not all dependencies"
    },
    "path": {
      "option": "-p, --path [path]",
      "description": "The path to target"
    }
  },
  "plugin": "./plugin.js"
}
