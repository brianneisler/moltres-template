{
  "name": "deploy",
  "command": "deploy",
  "description": "deploys a moltres project to firebase",
  "options": {
    "stage": {
      "option": "-s, --stage [name]",
      "description": "Set the stage",
      "default": "dev"
    }
  },
  "plugin": "./plugin.js"
}
