{
  "name": "build",
  "command": "build",
  "description": "builds a moltres project",
  "options": {
    "stage": {
      "option": "-s, --stage [name]",
      "description": "Set the stage",
      "default": "dev"
    }
  },
  "plugin": "./plugin.js"
}
