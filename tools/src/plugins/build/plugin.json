{
  "command": "build",
  "description": "builds a moltres project",
  "options": {
    "env": {
      "option": "-e, --env [env path]",
      "description": "Set the path to a .env file"
    },
    "only": {
      "option": "-o, --only",
      "description": "Only build the specified target and not all dependencies"
    },
    "path": {
      "option": "-p, --path [path]",
      "description": "The path to target"
    },
    "stage": {
      "option": "-s, --stage [name]",
      "description": "Set the stage",
      "default": "dev"
    }
  },
  "plugin": "./plugin.js"
}
